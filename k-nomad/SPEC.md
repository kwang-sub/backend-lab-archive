# SPEC.md

## 프로젝트 목표
- [ ] 암호화폐 대시보드를 도시 리스트 중심 UI로 점진 전환
- [ ] DB 없이 Mock 데이터만 사용
- [ ] 단계별(Phase) 독립 실행 가능하도록 작업 단위 분리

## 고정 의사결정
- [ ] 단계 수: 4개
- [ ] 좋아요/싫어요 규칙: 동시 가능(각 버튼 독립 토글)
- [ ] 필터 실제 적용 시점: Phase 2
- [ ] 기본 정렬: 좋아요(likes) 내림차순

## 공용 인터페이스/타입 변경
- [x] `CoinData` -> `CityData` 전환
- [x] `CityData` 최소 필드 정의
  - [x] `id: string`
  - [x] `name: string`
  - [x] `description?: string`
  - [x] `budget: "100만원" | "100~200만원" | "200만원"`
  - [x] `region: "전체" | "수도권" | "경상도" | "전라도" | "강원도" | "제주도" | "충청도"`
  - [x] `environment: "자연친화" | "도심선호" | "카페작업" | "코워킹 필수"`
  - [x] `bestSeason: "봄" | "여름" | "가을" | "겨울"`
  - [x] `likes: number`
  - [x] `dislikes: number`
  - [x] `userVote: { like: boolean; dislike: boolean }`
- [x] 필터 상태 타입 정의
  - [x] `FilterState = { budget?: ..., region?: ..., environment?: ..., bestSeason?: ... }`
- [x] 정렬 정책 확정
  - [x] 기본 정렬: `likes` 내림차순
  - [x] 동점 보조 정렬: 이름 오름차순(Phase 4에서 확정)

---

## Phase 1. 페이지 구조 정리 + 도시 도메인 전환 기반

### 오버뷰
- [x] 홈/인증 페이지만 남기고 라우팅 및 내비게이션 최소화
- [x] 암호화폐 테이블 UI를 도시 리스트 스켈레톤으로 전환

### 구현 체크리스트
- [x] 홈페이지(`/`)와 인증 페이지(`/login`, `/register`) 외 페이지 제거
- [x] 홈 헤더에서 인증/프로필 관련 버튼 제외 다른 페이지 이동 버튼 삭제
- [x] `crypto-ranking-board.tsx`를 도시 리스트 스켈레톤 구조로 교체
- [x] `TOP 추천 도시` 제목을 `도시 리스트`로 변경
- [x] `자세히 보기` 버튼(또는 동등 액션) 제거 기준 반영

### 검증 체크리스트
- [x] 접근 가능한 라우트가 `/`, `/login`, `/register`만 남아 있음
- [x] 불필요 내비게이션 버튼 제거 확인
- [x] 홈 타이틀이 `도시 리스트`로 노출됨
- [x] 라우트 참조 오류 없음

---

## Phase 2. 필터 체계 확정 + Mock 데이터 재설계 + 필터 동작 적용

### 오버뷰
- [x] 요구된 필터 스펙 고정
- [x] 도시 Mock 데이터에 필터 요소를 반영하고 실제 필터 동작 구현

### Execution Plan (Phase 2 전용)
- [x] 1단계: 필터 스펙/타입 고정
  - [x] `CityData`에 Phase 2 필터 필드 타입을 확정한다.
  - [x] 필터 옵션 상수(예산/지역/환경/최고 계절)를 명시적으로 고정한다.
  - [x] `FilterState`를 Phase 2 옵션 범위에 맞춰 단순 정의한다.
  - [x] `pnpm lint` 실행
  - [x] `pnpm exec tsc --noEmit` 실행
- [x] 2단계: Mock 데이터 재설계
  - [x] 모든 도시 Mock 데이터에 필터 요소(예산/지역/환경/최고 계절)를 최소 1세트씩 채운다.
  - [x] Mock 데이터 값이 허용 옵션 범위를 벗어나지 않도록 맞춘다.
  - [x] `pnpm lint` 실행
  - [x] `pnpm exec tsc --noEmit` 실행
- [x] 3단계: 필터 동작 적용
  - [x] 필터 상태 변경 시 리스트를 즉시 재계산하도록 적용한다.
  - [x] 복수 필터는 교집합 조건으로 처리한다.
  - [x] `지역=전체` 선택 시 지역 조건을 적용하지 않는다.
  - [x] 초기/전체 상태에서는 전체 도시가 노출되도록 유지한다.
  - [x] `pnpm lint` 실행
  - [x] `pnpm exec tsc --noEmit` 실행

### 구현 체크리스트
- [x] 필터 옵션 고정
  - [x] 예산: `100만원`, `100~200만원`, `200만원`
  - [x] 지역: `전체`, `수도권`, `경상도`, `전라도`, `강원도`, `제주도`, `충청도`
  - [x] 환경: `자연친화`, `도심선호`, `카페작업`, `코워킹 필수`
  - [x] 최고 계절: `봄`, `여름`, `가을`, `겨울`
- [x] 도시별 Mock 데이터에 필터 요소 최소 1세트 이상 추가
- [x] 필터 상태 변경 시 리스트 즉시 재계산
- [x] `지역=전체`는 지역 필터 미적용으로 처리

### 검증 체크리스트
- [x] 각 필터 단독 적용 시 결과 변경 확인
- [x] 복수 필터 조합 시 교집합 결과 확인
- [x] 초기/전체 상태에서 전체 도시 노출 확인
- [x] Mock 데이터 값이 허용 옵션 범위를 벗어나지 않음

---

## Phase 3. 카드 구조 개편 + 좋아요/싫어요 상호작용 구현

### 오버뷰
- [x] 카드 정보를 Key-Value 구조로 변경
- [x] 별점 제거 후 좋아요/싫어요 인터랙션 완성
- [x] 대시보드 필터 셀렉트의 가독성 중심 테마 개선 적용

### 구현 체크리스트
- [x] 별점/평점 UI 완전 제거
- [x] 카드에 Key-Value 표시 추가
  - [x] 예산
  - [x] 지역
  - [x] 환경
  - [x] 최고 계절
- [x] 좋아요/싫어요 버튼만 유지
- [x] 버튼 상태에 따라 아이콘 색상 변경
- [x] 버튼 토글 시 수치 증감 반영
  - [x] 좋아요 토글 on: +1 / off: -1
  - [x] 싫어요 토글 on: +1 / off: -1
  - [x] 좋아요/싫어요 동시 활성화 허용(독립 상태)
- [x] `자세히 보기` 버튼 최종 제거
- [x] 셀렉트 박스 Trigger/Option 대비 테마 적용
- [x] 선택값/옵션 텍스트가 배경과 구분되는 조화 색상 적용

### 검증 체크리스트
- [x] 좋아요 on/off 시 숫자 증감 정확성
- [x] 싫어요 on/off 시 숫자 증감 정확성
- [x] 동시 활성화 시 색상/카운트 독립 반영 확인
- [x] 카드 Key-Value 4개 누락 없음
- [x] 별점/자세히 보기 UI 제거 확인
- [x] 셀렉트 기본값/선택값/옵션 텍스트 가독성 확인
- [x] 셀렉트 포커스 상태에서 시인성 확인

---

## Phase 4. 정렬 규칙 완성 + 품질 검증 + 회귀 방지 테스트

### 오버뷰
- [x] 좋아요 순 정렬 규칙 완성
- [x] 핵심 시나리오 테스트 추가로 안정성 확보

### 구현 체크리스트
- [x] 리스트 기본 정렬을 좋아요 내림차순으로 고정
- [x] 좋아요 변경 시 정렬 즉시 재적용
- [x] 동점 시 이름 오름차순 보조 정렬 적용
- [x] 핵심 테스트 작성
  - [x] 필터 적용 테스트
  - [x] 좋아요/싫어요 토글 테스트
  - [x] 좋아요 기준 정렬 테스트
  - [x] 제거 대상 UI 부재 테스트(별점/자세히 보기/불필요 내비게이션)

### 검증 체크리스트
- [x] 좋아요 변경 직후 목록 재정렬 확인
- [x] 동점 정렬 규칙 일관성 확인
- [x] `pnpm test` 통과
- [x] 필요 시 `pnpm lint` 통과
- [ ] 로그인/홈/필터/투표 수동 시나리오 회귀 없음

---

## 통합 검증 시나리오
- [ ] 로그인 후 홈 진입 시 `도시 리스트` 노출
- [ ] 필터 4종이 지정 옵션으로 렌더링
- [ ] 도시 카드에 예산/지역/환경/최고 계절 표시
- [ ] 좋아요/싫어요 버튼만 존재, 색상/수치 토글 반영
- [ ] 목록이 좋아요 순으로 정렬
- [ ] 홈/인증 외 페이지 경로 제거

## 가정 및 기본값
- [ ] DB 미사용, 정적 Mock/클라이언트 메모리 상태 사용
- [ ] 사용자별 투표 영속성은 범위 밖(새로고침 시 초기화 허용)
- [ ] 인증 플로우 자체는 기존 방식 유지(내비게이션 최소화 중심)
